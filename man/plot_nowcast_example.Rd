% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_nowcast_example}
\alias{plot_nowcast_example}
\title{Plot the nowcasts for several dates}
\usage{
plot_nowcast_example(
  df_nowcast,
  df_total,
  dates_to_show,
  data_origin = c("case_study", "NegBinX", "NegBin2D", "NegBin1D"),
  save_plot = TRUE
)
}
\arguments{
\item{df_nowcast}{a list of data frames containing columns \code{Distribution}
(containing the name of the observation model), \code{quantile_50}
(the point nowcasts), \code{quantile_2.5}, \code{quantile_25}, \code{quantile_75},
\code{quantile_97.5} (bounds of the prediction intervals), \code{date} (x-axis
dates), \code{nowcast_date} (when the nowcast was issued) and \code{delay} (nowcast
horizon). The individual data frames in the list correspond to different
dates, when the nowcast is calculated.}

\item{df_total}{a list of data frames containing columns \code{date}, \code{counts} and
\code{data}. The last column \code{data} is an indicator, whether the values in the
\code{counts} column are the final sums of the counts, or the preliminary data
version. Needed to plot the observations alongside the nowcasts. The
individual data frames in the list correspond to different dates, when the
nowcast is calculated.}

\item{dates_to_show}{a selection of a few consecutive dates for which we want
to show the estimates.}

\item{data_origin}{a string indicating the data generating process of
simulated data, or whether the data correspond to the case study. Possible
values are "case_study", "NegBinX", "NegBin2D" and "NegBin1D"}

\item{save_plot}{logical indicator, whether to save the plot using
\code{ggplot2::ggsave()}}
}
\value{
a patchwork plot, or NULL if \code{save_plot = TRUE}
}
\description{
This function creates a patchwork picture composed of the
nowcast plots as calculated in "real time". Individual panels correspond
to different dates, when the nowcast is calculated.
}
