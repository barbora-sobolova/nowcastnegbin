% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_plot_data_frames.R
\name{filter_nowcast_example_dates}
\alias{filter_nowcast_example_dates}
\title{Extract estimates of the mean process and nowcasts for selected time windows}
\usage{
filter_nowcast_example_dates(
  df_nowcast_mcmc,
  df_nowcast_glm,
  df_total,
  df_lambda_mcmc = NULL,
  df_lambda_glm = NULL,
  dates_to_show = c("2019-03-24", "2019-03-31", "2019-04-07", "2019-04-14"),
  model_to_show = c("Poisson", "NegBinX", "NegBin2D", "NegBin1D", "NegBin2M", "NegBin1M")
)
}
\arguments{
\item{df_nowcast_mcmc}{a data frame with columns \code{week}, \code{.value},
\code{Distribution}, \code{nowcast_date} and \code{sensitivity_sc} that contains the
distribution of the nowcast obtained from the MCMC method in a sample format}

\item{df_nowcast_glm}{same as \code{df_nowcast_mcmc} but the results are
obtained from the GLM method}

\item{df_total}{a data frame containing columns \code{date}, \code{counts} and \code{data}.
The last column \code{data} is an indicator, whether the values in the \code{counts}
column are the final sums of the counts, or the preliminary data version.
Needed to plot the observations alongside the nowcasts.}

\item{df_lambda_mcmc}{a data frame with columns \code{week}, \code{.value},
\code{Distribution} and \code{nowcast_date}, that contains the distribution of the mean
process obtained from the MCMC method in a sample format. If NULL, the
distribution of the mean process is ignored.}

\item{df_lambda_glm}{same as \code{df_lambda_mcmc} but the results are
obtained from the GLM method}

\item{dates_to_show}{a selection of 4-6 consecutive dates for which we want
to show the estimates.}

\item{model_to_show}{a character vector indicating an observation models,
from which we want to show the estimates}
}
\value{
a list containing 3 data frames
\describe{
\item{\code{lambda}}{with columns \code{week} (the time as an integer starting
from 1), \code{.value} (sampled value of lambda), \code{Distribution} (observation
model name), \code{nowcast_date} (date when the nowcast was calculated),
\code{sensitivity_sc} (name of the sensitivity analysis scenario, applicable
only for the MCMC method) and \code{method} (either "mcmc" or "glm"),}
\item{\code{nowcast}}{with columns \code{date} (date of the nowcasting target),
\code{delay} (reporting delay in weeks), \code{nowcast_date}, \code{Distribution},
\code{quantile_2.5},\code{quantile_25}, \code{quantile_50}, \code{quantile_75}, \code{quantile_97.5}
and \code{method},}
\item{\code{total}}{a data frame containing columns \code{date}, \code{counts} and
\code{data} (indicator, whether the counts are preliminary or final).}
}
The returned data frames are empty, if the input data contained no entries
from the selected dates.
}
\description{
This function filters data frame rows corresponding to selected
dates and observation model.
}
