% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_trajectory}
\alias{plot_trajectory}
\title{Plot the whole incidence trajectory}
\usage{
plot_trajectory(
  full_data,
  start_date,
  length_of_train_data,
  max_lag,
  aux_study_start,
  data_origin = c("case_study", "NegBinX", "NegBin2D", "NegBin1D"),
  save_plot = TRUE
)
}
\arguments{
\item{full_data}{a data frame of the whole trajectory containing columns
\code{date} and columns \code{value_0w}, \code{value_1w}, etc. until \code{max_lag - 1}.}

\item{start_date}{a date (indeed in the date format), where the training data
start. The starting point will be included.}

\item{length_of_train_data}{a number, the length of the estimation window
(endpoints included)}

\item{max_lag}{maximum reporting delay represented by the number of columns
of the reporting table. In this way, the 0-th lag counts as the first, 1-st
lag as the second and so on.}

\item{aux_study_start}{a date (indeed in the date format), where the
auxiliary case study period used for determining the priors starts.}

\item{data_origin}{a string indicating the data generating process of
simulated data, or whether the data correspond to the case study. Possible
values are "case_study", "NegBinX", "NegBin2D" and "NegBin1D"}

\item{save_plot}{logical indicator, whether to save the plot using
\code{ggplot2::ggsave()}}
}
\value{
a ggplot object, or NULL if \code{save_plot = TRUE}
}
\description{
This function plots and possibly saves the incidence trajectory
used for the case study. The first and the last estimation windows will be
highlighted to see the chunk of the data we use for model training.
}
