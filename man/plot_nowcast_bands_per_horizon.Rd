% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_nowcast_bands_per_horizon}
\alias{plot_nowcast_bands_per_horizon}
\title{Plot the prediction bands around the trajectory per horizon}
\usage{
plot_nowcast_bands_per_horizon(
  true_data,
  prelim_data,
  df_nowcast,
  start_date,
  end_date,
  skip_dates,
  horizon
)
}
\arguments{
\item{true_data}{a vector of the final state of the incidence time series}

\item{prelim_data}{a vector of the preliminary state of the incidence time
series, which is the sum of partial counts until \code{delay}}

\item{df_nowcast}{a data frame containing columns \code{Distribution}
(containing the name of the observation model), \code{quantile_50}
(the point nowcasts), \code{quantile_2.5}, \code{quantile_25}, \code{quantile_75},
\code{quantile_97.5} (bounds of the prediction intervals), \code{date} (x-axis
dates) and \code{nowcast_date} (when the nowcast was issued)}

\item{start_date}{a date (in the date format), where the nowcasting starts.
The starting point will be included.}

\item{end_date}{a date (in the date format), where the nowcasting ends.
The ending point will be included.}

\item{skip_dates}{a vector of dates, where no nowcasting has been done and
where we should leave gaps in the plot of the nowcasts.
\code{skip_dates = NULL} if no gaps are to be plotted.}

\item{horizon}{integer, the data up to this reporting delay are included in
the preliminary data}
}
\value{
a ggplot object
}
\description{
This function plots the incidence trajectory with the prediction
intervals as bands around the observed data. The plot is created for one
specific nowcasting horizon.
}
