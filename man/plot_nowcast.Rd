% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_nowcast}
\alias{plot_nowcast}
\title{Plot and save the nowcasts}
\usage{
plot_nowcast(
  df_summarized_nowcast,
  df_total,
  model_codes,
  model_colors,
  nowcast_date,
  save_plot = TRUE
)
}
\arguments{
\item{df_summarized_nowcast}{a data frame containing columns \code{Distribution}
(containing the name of the observation model), \code{quantile_50}
(the point nowcasts), \code{quantile_2.5}, \code{quantile_25}, \code{quantile_75},
\code{quantile_97.5} (bounds of the prediction intervals) and \code{date} (x-axis
dates)}

\item{df_total}{a data frame}

\item{model_codes}{a vector of observation model names. Must be in the
correct order to label the models correctly. The order in case all models are
used is: "Poisson", "NegBinX", "NegBin2D", "NegBin1D", "NegBin2M",
"NegBin1M".}

\item{model_colors}{a named vector of the model colors corresponding to each
observation model}

\item{nowcast_date}{a date, when the nowcast is made}

\item{save_plot}{logical indicator, whether to save the plot using
\code{ggplot2::ggsave()}}
}
\value{
a ggplot object with one facet per observation model
}
\description{
This function plots and possibly saves the nowcasts from all
observation models for a single date. The nowcasts are plotted along the
data for comparison.
}
