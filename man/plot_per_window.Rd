% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_per_window}
\alias{plot_per_window}
\title{A wrapper around plotting functions creating all relevant per-window plots}
\usage{
plot_per_window(
  df_nowcast,
  df_delay_prob,
  df_disp_par,
  df_rw_sd,
  df_total,
  model_names,
  date_of_the_nowcast,
  fitting_method = c("mcmc", "glm"),
  prob_prior_pars = NULL,
  disp_prior_pars = NULL,
  save_plot = TRUE
)
}
\arguments{
\item{df_nowcast}{a data frame containing columns \code{Distribution}
(containing the name of the observation model), \code{quantile_50}
(the point nowcasts), \code{quantile_2.5}, \code{quantile_25}, \code{quantile_75},
\code{quantile_97.5} (bounds of the prediction intervals) and \code{date} (x-axis
dates)}

\item{df_delay_prob}{a data frame containing columns \code{Distribution}
(containing the name of the observation model), \code{.value} (the empirical
distribution of the delay probability estimates), \code{nowcast_date} (the
date when the nowcast is calculated) and \code{delay} (the discrete delay time)}

\item{df_disp_par}{a data frame containing columns \code{Distribution}
(containing the name of the observation model), \code{.value} (the empirical
distribution of the dispersion parameter estimates) and \code{nowcast_date} (the
date when the nowcast is calculated)}

\item{df_rw_sd}{a data frame containing columns \code{Distribution}
(containing the name of the observation model), \code{.value} (the empirical
distribution of the estimates of the random walk standard deviation) and
\code{nowcast_date} (the date when the nowcast is calculated)}

\item{df_total}{a data frame with columns \code{date}, \code{counts} and \code{data}
returned by the function \code{create_totals_data_frame()}}

\item{model_names}{a vector of names of the observation models, we wish to
plot.}

\item{date_of_the_nowcast}{a date, when the nowcast is made to name saved
files correctly}

\item{fitting_method}{a method used for fitting the nowcasting model, either
"mcmc", or "glm"}

\item{prob_prior_pars}{a vector of the prior parameters of the Dirichlet
delay probability distribution}

\item{disp_prior_pars}{a data frame with columns \code{model_name}, \code{mean_log} and
\code{sd_log}, which contain the parameters for the prior log-normal distribution
of the dispersion parametr. The data frame should have 6 rows, one for each
model}

\item{save_plot}{logical indicator, whether to save the plot using
\code{ggsave()}}
}
\value{
a list of ggplot objects or list of NULLs if \code{save_plot = TRUE}
}
\description{
This is a wrapper around functions that plot the posterior
distributions of parameters in each rolling-window: \code{plot_nowcast()},
\code{plot_delay_prob()}, \code{plot_disp_par} and in the case of the MCMC
method also \code{plot_rw_sd()}.
}
