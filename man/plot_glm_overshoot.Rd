% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_glm_overshoot}
\alias{plot_glm_overshoot}
\title{Plot the nowcasts, where the GLM method overshoots}
\usage{
plot_glm_overshoot(
  df_nowcast,
  df_lambda,
  df_total,
  dates_to_show,
  model_to_show = "NegBinX",
  save_plot = TRUE
)
}
\arguments{
\item{df_nowcast}{a data frame with columns \code{date} (date of the
nowcasting target), \code{delay} (reporting delay in weeks), \code{nowcast_date},
\code{Distribution}, \code{quantile_2.5},\code{quantile_25}, \code{quantile_50}, \code{quantile_75},
\code{quantile_97.5} and \code{method}, that contains summarized nowcasting results
from both the MCMC and GLM method}

\item{df_lambda}{a data frame with columns \code{week}, \code{.value},
\code{Distribution} and \code{nowcast_date}, that contains the distribution of the mean
process in a sample format}

\item{df_total}{a data frame containing columns \code{date}, \code{counts} and \code{data}.
The last column \code{data} is an indicator, whether the values in the \code{counts}
column are the final sums of the counts, or the preliminary data version.
Needed to plot the observations alongside the nowcasts.}

\item{dates_to_show}{a selection of 4-6 consecutive dates for which we want
to show the estimates.}

\item{model_to_show}{a string indicating an observation model, from which we
want to show th estimates}

\item{save_plot}{logical indicator, whether to save the plot using
\code{ggplot2::ggsave()}}
}
\value{
a ggplot object, or NULL if \code{save_plot = TRUE}
}
\description{
This function plots and possibly saves the plot of nowcasts and
estimate of the mean process \eqn{\lambda_t} for selected dates to highlight
the shortcomings of the GLM method leading to somewhat worse results compared
to the MCMC method.
}
