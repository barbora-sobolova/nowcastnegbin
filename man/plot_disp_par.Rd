% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_disp_par}
\alias{plot_disp_par}
\title{Plot and save the density plot of the dispersion parameter estimates}
\usage{
plot_disp_par(
  df_nb_size,
  model_names,
  date_of_the_nowcast,
  fitting_method = c("mcmc", "glm"),
  disp_prior_pars = NULL,
  data_origin = c("SARI", "ILI", "NegBinX", "NegBin2D", "NegBin1D"),
  sensitivity_sc = "",
  true_value = NULL,
  save_plot = TRUE
)
}
\arguments{
\item{df_nb_size}{a data frame containing columns \code{Distribution}
(containing the name of the observation model), \code{.value} (the empirical
distribution of the dispersion parameter estimates) and \code{nowcast_date} (the
date when the nowcast is calculated)}

\item{model_names}{a vector of names of the observation models, we wish to
plot.}

\item{date_of_the_nowcast}{a date, when the nowcast is made to name the
saved file correctly}

\item{fitting_method}{a method used for fitting the nowcasting model, either
"mcmc", or "glm"}

\item{disp_prior_pars}{a data frame with columns \code{model_name}, \code{mean_log} and
\code{sd_log}, which contain the parameters for the prior log-normal distribution
of the dispersion parameter. The data frame should have 6 rows, one for each
model, although only the NegBinX, NegBin2D and NegBin1D rows will be used.
NegBin2M shares the prior with NegBinX and NegBin1M has the same prior as
NegBin1D.}

\item{data_origin}{a string indicating the data generating process of
simulated data, or the corresponding case study. Possible
values are "SARI", "ILI", "NegBinX", "NegBin2D" and "NegBin1D"}

\item{sensitivity_sc}{a string indicating the sensitivity analysis scenario
of the MCMC method. Empty string "" indicates the main analysis.}

\item{true_value}{NULL for \code{data_origin} equal "SARI", or "ILI",
otherwise the true value of the delay probability vector used to generate the
data}

\item{save_plot}{logical indicator, whether to save the plot using
\code{ggsave()}}
}
\value{
a ggplot object with one facet showing the density of the dispersion
parameter estimates, or NULL if \code{save_plot = TRUE}
}
\description{
This function plots and possibly saves the densities of
dispersion parameter estimates for all fitted negative binomial models.
}
