% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_all_glm_models}
\alias{fit_all_glm_models}
\title{Fit multiple GLM nowcasting models}
\usage{
fit_all_glm_models(
  stan_data,
  date_of_the_nowcast,
  model_name,
  n_samples = 4000
)
}
\arguments{
\item{stan_data}{a list of data and parameters accepted by the STAN model
returned by the \code{get_stan_data()} function. This list is reused here to
create a data frame for the regression model.}

\item{date_of_the_nowcast}{a vector of dates, when the nowcast is made}

\item{model_name}{a character vector indicating observation models to be
fit. Must be of the same length as \code{date_of_the_nowcast}}

\item{n_samples}{how many samples from the nowcasting distribution we draw}
}
\value{
List of the data frames with the draws of different model parameters
and additional quantities:
\describe{
\item{\code{nowcast}}{samples from the nowcasting distribution,}
\item{\code{lambda}}{samples of the mean incidence trajectory,}
\item{\code{delay_prob}}{samples of the delay probability vector,}
\item{\code{nb_size}}{The draws of the size parameter of the negative
binomial distribution. Not applicable for the Poisson model,}
\item{\code{log_disp_coeff}}{a vector with two elements, or a data frame
with two columns: \code{log_disp_hat} containing the point estimate of the
dispersion parameter on the log scale and \code{log_disp_sd} its standard
deviation (also on the log scale). The dispersion is the reciprocal of the
negative binomial size here, i.e. 0 loosely means Poisson model, while
higher values indicate higher dispersion,}
\item{\code{iter}}{a scalar, the number of iterations necessary for fitting
the gamlss model.}
}
The columns of the data frames are a subset of the columns of the
corresponding data frame returned by \code{fit_stan_model}. Columns \code{.chain}
and \code{.iteration}, that are relevant only for the MCMC sampling, are not
present here. The return list is designed similarly as the output of
\code{fit_stan_model()} to facilitate the processing and plotting of the
results.
}
\description{
This is a wrapper around \code{fit_glm_model()} that allows fitting
for multiple observation models and also possibly multiple time points. The
purpose of this function is to return the fits from different observation
models for one date in a single bundle to simplify grouping structures in the
targets pipeline.
}
