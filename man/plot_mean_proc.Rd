% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_mean_proc}
\alias{plot_mean_proc}
\title{Plot and save the density plot of the mean process estimates}
\usage{
plot_mean_proc(
  df_lambda,
  model_names,
  date_of_the_nowcast,
  max_lag,
  fitting_method = c("mcmc", "glm"),
  data_origin = c("case_study", "NegBinX", "NegBin2D", "NegBin1D"),
  true_value = NULL,
  save_plot = TRUE
)
}
\arguments{
\item{df_lambda}{a data frame containing columns \code{Distribution}
(containing the name of the observation model), \code{.value} (the empirical
distribution of the delay probability estimates), \code{nowcast_date} (the
date when the nowcast is calculated) and \code{week} (the week number from the
beginning of the rolling window)}

\item{model_names}{a vector of names of the observation models, we wish to
plot.}

\item{date_of_the_nowcast}{a date, when the nowcast is made to name the
saved file correctly}

\item{max_lag}{an integer indicating the number of columns of the reporting
triangle}

\item{fitting_method}{a method used for fitting the nowcasting model, either
"mcmc", or "glm"}

\item{data_origin}{a string indicating the data generating process of
simulated data, or whether the data correspond to the case study. Possible
values are "case_study", "NegBinX", "NegBin2D" and "NegBin1D"}

\item{true_value}{NULL for \code{data_origin = "case_study"}, otherwise the
vector of true values of the mean of the total counts used to generate the
data. The length of the vector must be \code{max_lag - 1}}

\item{save_plot}{logical indicator, whether to save the plot using
\code{ggsave()}}
}
\value{
a ggplot object with one facet per week showing the density of the
estimates of the mean process, or NULL if \code{save_plot = TRUE}
}
\description{
This function plots and possibly saves the densities of
estimates of the mean value of the total counts for all fitted models.
}
